node_modules/
.env
.env.local
build/
dist/
.next/
.DS_Store
Pods/
DerivedData/
*.xcworkspace
.next/
next-env.d.ts

.beads/
.claude/worktrees/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Infra local secrets (never commit tokens / connection strings)
infra/**/project.env
infra/**/*.local.env

# TypeScript incremental build artifact
tsconfig.tsbuildinfo

# tsvad model weights (downloaded, not committed)
public/models/*.onnx
samples/
